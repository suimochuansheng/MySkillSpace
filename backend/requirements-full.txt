# ==================== GPU/训练加速（本地大模型专用）====================
# accelerate==1.12.0  # PyTorch 训练加速（API 不需要）

aiohappyeyeballs==2.4.3
aiohttp==3.10.11
aiosignal==1.3.1
amqp==5.3.1
annotated-types==0.7.0
anyio==4.12.0
asgiref==3.11.0
async-timeout==5.0.1
attrs==24.2.0
billiard==4.2.4

# bitsandbytes==0.49.0  # 4-bit/8-bit 量化（API 不需要）
celery==5.6.0
certifi==2025.11.12
cffi==2.0.0
charset-normalizer==3.4.0
click==8.1.7
click-didyoumean==0.3.1
click-plugins==1.1.1.2
click-repl==0.3.0
colorama==0.4.6
cryptography==46.0.3

# ==================== 模型下载/管理（本地大模型专用）====================
# dashscope==1.25.3  # 阿里云 ModelScope SDK（API 不需要）

distro==1.9.0
Django==4.2.27
django-cors-headers==4.9.0
django-ratelimit==4.1.0
django-simpleui==2025.5.262
djangorestframework==3.16.1
exceptiongroup==1.3.1
filelock==3.20.0
flower==2.0.1
frozenlist==1.8.0
fsspec==2025.12.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1

# huggingface-hub==0.36.0  # HuggingFace 模型下载（API 不需要）

humanize==4.14.0
idna==3.10
Jinja2==3.1.4
jiter==0.12.0
kombu==5.6.1
MarkupSafe==3.0.2

# modelscope==1.33.0  # ModelScope 模型下载（API 不需要）
# mpmath==1.3.0  # 符号计算（sympy 依赖，API 不需要）

multidict==6.7.0
networkx==3.6.1
numpy==1.26.4
openai==2.11.0
packaging==25.0
pandas==2.2.2
pdfminer.six==20251107
pdfplumber==0.11.8

# peft==0.18.0  # 参数高效微调（API 不需要）

pillow==11.0.0
paramiko==3.5.0  # SSH客户端库（云服务器远程监控）
docker==7.1.0  # Docker SDK for Python（容器监控）
prometheus_client==0.23.1
prompt_toolkit==3.0.52
propcache==0.4.1
psutil==7.1.3
pycparser==2.23
pydantic==2.9.2
pydantic_core==2.23.4
PyMySQL==1.1.2
pypdfium2==5.1.0
python-dateutil==2.9.0.post0
python-dotenv==1.2.1
pytz==2025.2
PyYAML==6.0.3
redis==7.1.0
regex==2025.11.3
requests==2.32.5

# ==================== 模型文件/科学计算（本地大模型专用）====================
# safetensors==0.7.0  # 模型文件格式（API 不需要）
# scipy==1.13.1  # 科学计算（transformers 依赖，API 不需要）
# sentencepiece==0.2.1  # 分词器（API 不需要）

six==1.17.0
sniffio==1.3.1
sqlparse==0.5.3

# sympy==1.14.0  # 符号计算（API 不需要）

tiktoken==0.12.0

# ==================== 分词器/Transformer 核心（本地大模型专用）====================
# tokenizers==0.22.1  # HuggingFace 分词器（API 不需要）

# --- 核心 AI 依赖 (请根据平台手动安装) ---
# Windows/Linux (GPU): pip install torch==2.4.1+cu124 ... --index-url https://download.pytorch.org/whl/cu124
# +cu124版本是本地显卡需要的版本, 请根据自己的显卡型号替换
# torch==2.4.1  # PyTorch（本地 GPU 推理需要，2GB+）
# torchvision==0.19.1  # 计算机视觉（本地需要）
# torchaudio==2.4.1  # 音频处理（本地需要）

tornado==6.5.3
tqdm==4.67.1

# transformers==4.57.3  # Transformer 模型库（本地大模型需要，500MB+）
# transformers-stream-generator==0.0.4  # 流式生成（本地需要）

typing_extensions==4.12.2
tzdata==2025.2
tzlocal==5.3.1
urllib3==2.6.2
vine==5.1.0
wcwidth==0.2.14
websocket-client==1.9.0
xxhash==3.5.0
yarl==1.22.0

# ==================== Django Channels (WebSocket 支持) ====================
channels==4.0.0
channels-redis==4.1.0
daphne==4.0.0

# ==================== 说明 ====================
# 本文件为云端 API 模式优化版本
# 已注释掉的包仅在本地大模型模式下需要，包括：
# - GPU 训练加速：accelerate, bitsandbytes, peft
# - 模型下载：huggingface-hub, modelscope, dashscope
# - Transformer 核心：transformers, transformers-stream-generator, tokenizers
# - 分词器：sentencepiece, tokenizers
# - 模型文件：safetensors
# - 科学计算：scipy, sympy, mpmath
# - PyTorch：torch, torchvision, torchaudio
#
# 保留的核心包：
# - openai：阿里云 API 调用
# - tiktoken：OpenAI tokenizer
# - 所有 Django/DRF/Celery/Redis 依赖
# - PDF 处理、网络请求等通用工具
#
# 预计镜像大小：从 13GB 减少到 2-3GB
