# ==================== CD（持续部署）工作流 ====================
# 功能：构建Docker镜像并推送到阿里云容器镜像服务（ACR）
# 触发：当代码推送到 main/master 分支时
# 优化：智能检测文件变化，按需构建镜像（节省时间和资源）

name: CD - 构建并推送镜像

# 触发条件
on:
  push:
    branches: [ "main", "master" ]
    paths:  # 只有这些文件变化时才触发
      - 'frontend/skillspace/**'
      - 'backend/**'
      - 'docker-compose.prod.yml'
      - '.github/workflows/cd.yml'

  # 支持手动触发（方便测试）
  workflow_dispatch:

# 环境变量（全局）
env:
  ACR_REGISTRY: ${{ secrets.ACR_REGISTRY }}
  ACR_NAMESPACE: ${{ secrets.ACR_NAMESPACE }}

jobs:
  # ==================== 任务0：检测文件变化 ====================
  detect-changes:
    name: 检测文件变化
    runs-on: ubuntu-latest
    outputs:
      backend: ${{ steps.filter.outputs.backend }}
      frontend: ${{ steps.filter.outputs.frontend }}
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 检测哪些模块发生了变化
        uses: dorny/paths-filter@v2
        id: filter
        with:
          filters: |
            backend:
              - 'backend/**'
              - 'docker-compose.prod.yml'
            frontend:
              - 'frontend/skillspace/**'

  # ==================== 任务1：构建后端镜像 ====================
  build-backend:
    needs: detect-changes
    if: needs.detect-changes.outputs.backend == 'true'
    name: 构建后端镜像
    runs-on: ubuntu-latest

    steps:
      # 1. 检出代码
      - name: 检出代码仓库
        uses: actions/checkout@v4

      # 2. 设置 Docker Buildx（支持缓存和多平台构建）
      - name: 设置 Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 3. 登录阿里云容器镜像服务
      - name: 登录阿里云 ACR
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.ACR_REGISTRY }}
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      # 4. 构建并推送后端镜像
      - name: 构建并推送后端镜像
        uses: docker/build-push-action@v5
        with:
          context: ./backend  # 构建上下文目录
          file: ./backend/Dockerfile.prod  # Dockerfile路径
          push: true  # 推送到远程仓库
          tags: |
            ${{ env.ACR_REGISTRY }}/${{ env.ACR_NAMESPACE }}/backend:latest
            ${{ env.ACR_REGISTRY }}/${{ env.ACR_NAMESPACE }}/backend:${{ github.sha }}
          # 缓存配置（加速后续构建）
          cache-from: type=gha
          cache-to: type=gha,mode=max
          # 构建参数
          build-args: |
            BUILDKIT_INLINE_CACHE=1

      # 5. 输出镜像信息
      - name: 输出镜像信息
        run: |
          echo "✅ 后端镜像构建成功！"
          echo "📦 镜像地址: ${{ env.ACR_REGISTRY }}/${{ env.ACR_NAMESPACE }}/backend:latest"
          echo "🏷️ Git SHA: ${{ github.sha }}"

  # ==================== 任务2：构建前端镜像 ====================
  build-frontend:
    needs: detect-changes
    if: needs.detect-changes.outputs.frontend == 'true'
    name: 构建前端镜像
    runs-on: ubuntu-latest

    steps:
      # 1. 检出代码
      - name: 检出代码仓库
        uses: actions/checkout@v4

      # 2. 设置 Docker Buildx
      - name: 设置 Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 3. 登录阿里云容器镜像服务
      - name: 登录阿里云 ACR
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.ACR_REGISTRY }}
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      # 4. 构建并推送前端镜像
      - name: 构建并推送前端镜像
        uses: docker/build-push-action@v5
        with:
          context: ./frontend/skillspace  # 构建上下文目录
          file: ./frontend/skillspace/Dockerfile  # Dockerfile路径
          push: true  # 推送到远程仓库
          tags: |
            ${{ env.ACR_REGISTRY }}/${{ env.ACR_NAMESPACE }}/frontend:latest
            ${{ env.ACR_REGISTRY }}/${{ env.ACR_NAMESPACE }}/frontend:${{ github.sha }}
          # 缓存配置（加速后续构建）
          cache-from: type=gha
          cache-to: type=gha,mode=max
          # 构建参数
          build-args: |
            BUILDKIT_INLINE_CACHE=1

      # 5. 输出镜像信息
      - name: 输出镜像信息
        run: |
          echo "✅ 前端镜像构建成功！"
          echo "📦 镜像地址: ${{ env.ACR_REGISTRY }}/${{ env.ACR_NAMESPACE }}/frontend:latest"
          echo "🏷️ Git SHA: ${{ github.sha }}"

  # ==================== 任务3：构建 Celery Worker 镜像 ====================
  build-celery-worker:
    needs: detect-changes
    if: needs.detect-changes.outputs.backend == 'true'
    name: 构建 Celery Worker 镜像
    runs-on: ubuntu-latest

    steps:
      # 1. 检出代码
      - name: 检出代码仓库
        uses: actions/checkout@v4

      # 2. 设置 Docker Buildx
      - name: 设置 Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 3. 登录阿里云容器镜像服务
      - name: 登录阿里云 ACR
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.ACR_REGISTRY }}
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      # 4. 构建并推送 Celery Worker 镜像
      - name: 构建并推送 Celery Worker 镜像
        uses: docker/build-push-action@v5
        with:
          context: ./backend  # 构建上下文目录
          file: ./backend/Dockerfile.prod  # Dockerfile路径
          push: true  # 推送到远程仓库
          tags: |
            ${{ env.ACR_REGISTRY }}/${{ env.ACR_NAMESPACE }}/celery_worker:latest
            ${{ env.ACR_REGISTRY }}/${{ env.ACR_NAMESPACE }}/celery_worker:${{ github.sha }}
          # 缓存配置（加速后续构建）
          cache-from: type=gha
          cache-to: type=gha,mode=max
          # 构建参数
          build-args: |
            BUILDKIT_INLINE_CACHE=1

      # 5. 输出镜像信息
      - name: 输出镜像信息
        run: |
          echo "✅ Celery Worker 镜像构建成功！"
          echo "📦 镜像地址: ${{ env.ACR_REGISTRY }}/${{ env.ACR_NAMESPACE }}/celery_worker:latest"
          echo "🏷️ Git SHA: ${{ github.sha }}"

  # ==================== 任务4：自动部署到服务器 ====================
  deploy:
    name: 部署到阿里云 ECS
    runs-on: ubuntu-latest
    needs: [build-backend, build-frontend, build-celery-worker]
    if: always() && (needs.build-backend.result == 'success' || needs.build-frontend.result == 'success' || needs.build-celery-worker.result == 'success')

    steps:
      # 1. SSH 连接并部署
      - name: SSH 连接并部署到服务器
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT }}
          script: |
            echo "========================================="
            echo "🚀 开始部署 SkillSpace 项目"
            echo "========================================="

            # 进入项目目录
            cd ${{ secrets.DEPLOY_PATH }}

            echo "📥 登录阿里云容器镜像服务..."
            echo "${{ secrets.ACR_PASSWORD }}" | docker login \
              --username ${{ secrets.ACR_USERNAME }} \
              --password-stdin \
              ${{ secrets.ACR_REGISTRY }}

            echo "📦 拉取最新镜像..."
            docker compose -f docker-compose.prod.yml pull

            echo "🔄 重启服务..."
            docker compose -f docker-compose.prod.yml up -d

            echo "🧹 清理未使用的镜像..."
            docker image prune -af --filter "until=24h"

            echo "✅ 部署完成！"
            echo "========================================="
            echo "📊 当前运行的服务："
            docker compose -f docker-compose.prod.yml ps
            echo "========================================="

      # 2. 输出部署结果
      - name: 输出部署总结
        run: |
          echo "========================================="
          echo "🎉 自动部署流程完成！"
          echo "========================================="
          echo ""
          echo "📦 本次构建的镜像："
          if [[ "${{ needs.build-backend.result }}" == "success" ]]; then
            echo "  ✅ 后端: ${{ env.ACR_REGISTRY }}/${{ env.ACR_NAMESPACE }}/backend:latest"
          else
            echo "  ⏭️  后端: 未变化，跳过构建"
          fi
          if [[ "${{ needs.build-frontend.result }}" == "success" ]]; then
            echo "  ✅ 前端: ${{ env.ACR_REGISTRY }}/${{ env.ACR_NAMESPACE }}/frontend:latest"
          else
            echo "  ⏭️  前端: 未变化，跳过构建"
          fi
          if [[ "${{ needs.build-celery-worker.result }}" == "success" ]]; then
            echo "  ✅ Celery Worker: ${{ env.ACR_REGISTRY }}/${{ env.ACR_NAMESPACE }}/celery_worker:latest"
          else
            echo "  ⏭️  Celery Worker: 未变化，跳过构建"
          fi
          echo ""
          echo "🔗 访问地址："
          echo "  - 请查看服务器配置的域名或 IP"
          echo ""
          echo "========================================="
