# ========================================
# SkillSpace 项目持续集成工作流配置
# CI/CD Pipeline for SkillSpace Project
# ========================================
# 技术栈：
#   前端：Vue 3 + Vite
#   后端：Django + DRF + MySQL + Redis
#   环境：Ubuntu Latest
# ========================================

name: SkillSpace CI/CD Pipeline

# ========================================
# 工作流触发条件
# ========================================
on:
  # 仅推核心分支（main/hotfix/*）时触发push事件（避免feature分支频繁push触发CI）
  push:
    branches: [ "main", "hotfix/*" ]
  # 所有向「main/feature/*/fix/*/hotfix/*」提PR时触发（覆盖所有开发场景）
  pull_request:
    branches: [ "main", "feature/*", "fix/*", "hotfix/*" ]
  # 保留手动触发
  workflow_dispatch:

# ========================================
# 全局环境变量
# ========================================
env:
  PYTHON_VERSION: '3.9'
  NODE_VERSION: '20'
  FRONTEND_BUILD_DIR: frontend/skillspace/dist
  BACKEND_DIR: backend

jobs:
  # ========================================
  # 作业1：前端测试和构建
  # ========================================
  frontend-test-build:
    name: 前端测试与构建 (Vue 3)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend/skillspace
    
    steps:
      - name: 📥 检出代码
        uses: actions/checkout@v4

            # === 添加这一行来验证 ===
      - name: 🚩 身份验证
        run: echo "我是新版本的CI文件！！！版本号 v2.0.1-sed"

      - name: 🟢 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: frontend/skillspace/package-lock.json
      
      - name: 📦 安装依赖
        run: npm ci --prefer-offline --no-audit
      
      # 预留：代码检查与单元测试
      # - run: npm run lint
      # - run: npm run test:unit
      
      - name: 🏗️ 构建生产版本
        run: npm run build
      
      - name: 📤 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dist
          path: ${{ env.FRONTEND_BUILD_DIR }}
          retention-days: 7

  # ========================================
  # 作业2：后端测试 (集成 MySQL & Redis)
  # ========================================
  backend-test:
    name: 后端测试 (Django + MySQL)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    
    # ---------------------------------------------------
    # 定义服务容器 (Service Containers)
    # GitHub Actions 会自动启动这些 Docker 容器并映射端口
    # ---------------------------------------------------
    services:
      # MySQL 8.0 服务
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: test_skillspace
        ports:
          - 3306:3306
        # 健康检查：确保数据库就绪后再运行后续步骤
        options: >-
          --health-cmd "mysqladmin ping -h localhost"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      # Redis 服务 (用于 Celery 测试)
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: 📥 检出代码
        uses: actions/checkout@v4
      
      - name: 🐍 设置 Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
          cache-dependency-path: backend/requirements.txt
      
      # ---------------------------------------------------
      # 安装依赖 (精确剔除 torch 相关包的声明（只匹配以 torch 开头的包）,含 PyTorch CPU 版修复)
      # ---------------------------------------------------
      - name: 📦 安装后端依赖
        run: |
          echo "升级pip..."
          python -m pip install --upgrade pip
          
          echo "🔧 正在适配 CI 环境 (使用CPU版PyTorch)..."
          # 1. 从 requirements.txt 中移除 GPU 版 torch
          sed -i '/torch==/d' requirements.txt
          sed -i '/torchvision==/d' requirements.txt
          sed -i '/torchaudio==/d' requirements.txt
          
          # 2. 手动安装 CPU 版本 PyTorch (加速 CI 下载)
          pip install torch==2.4.1 torchvision==0.19.1 torchaudio==2.4.1 --index-url https://download.pytorch.org/whl/cpu
          
          echo "安装其他依赖..."
          pip install -r requirements.txt

      # - name: 📦 安装后端依赖
      #   run: |
      #     echo "升级 pip..."
      #     python -m pip install --upgrade pip
          
      #     echo "🔧 处理依赖文件..."
      #     # 1. 剔除原文件中的 torch 相关行（避免 +cu124 版本报错）
      #     grep -v "torch" requirements.txt > requirements.ci.txt
          
      #     # 2. 安装 CPU 版 PyTorch (速度快，无驱动依赖)
      #     echo "⬇️ 安装 PyTorch (CPU)..."
      #     pip install torch==2.4.1 torchvision==0.19.1 torchaudio==2.4.1 --index-url https://download.pytorch.org/whl/cpu
          
      #     # 3. 安装其余项目依赖
      #     echo "⬇️ 安装项目依赖..."
      #     pip install -r requirements.ci.txt
          
      #     # 4. 【重要】安装 MySQL 驱动 (CI环境必须手动补充)
      #     pip install mysqlclient pkgconfig
          
      #     echo "✅ 依赖安装完成"

      # ---------------------------------------------------
      # 配置环境变量 (连接 Service 容器)
      # ---------------------------------------------------
      - name: ⚙️ 配置测试环境变量
        run: |
          cat > .env << EOF
          # --- 基础配置 ---
          DEBUG=False
          SECRET_KEY=ci-test-secret-key-must-be-long-enough
          
          # --- 关键：标识测试模式 (用于 Mock AI 模型) ---
          TESTING_MODE=True
          
          # --- 数据库配置 (连接 MySQL Service) ---
          # 注意：Service 在 GitHub Actions 中映射到 localhost
          DB_ENGINE=django.db.backends.mysql
          DB_NAME=test_skillspace
          DB_USER=root
          DB_PASSWORD=root
          DB_HOST=127.0.0.1
          DB_PORT=3306
          
          # --- Celery 配置 ---
          CELERY_BROKER_URL=redis://localhost:6379/0
          CELERY_RESULT_BACKEND=redis://localhost:6379/0
          EOF
          
          echo "✅ 环境变量已注入 (.env)"

      # - name: 🧐 验证Django安装
      #   run: |
      #       python -c "import django; print('Django版本:', django.get_version())"

      - name: 🔍 Django 系统检查
        run: python manage.py check

      - name: 🗄️ 执行数据库迁移
        run: |
          # 这一步验证了能否成功连接到 MySQL 容器
          python manage.py migrate

      - name: 🧪 运行单元测试
        # 使用主 settings，配合环境变量工作
        run: python manage.py test --verbosity=2
        env:
          DJANGO_SETTINGS_MODULE: SkillSpace.settings

  # ========================================
  # 作业3：代码质量检查 (并行)
  # ========================================
  code-quality:
    name: 代码质量检查
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 🔍 运行检查 (占位)
        run: echo "此处集成 Flake8 / Black / ESLint"

  # ========================================
  # 作业4：构建成功通知
  # ========================================
  build-success:
    name: ✅ 构建成功
    runs-on: ubuntu-latest
    needs: [frontend-test-build, backend-test, code-quality]
    steps:
      - name: 🎉 通知
        run: echo "CI Pipeline 全部通过，准备就绪！"

# ========================================
# CD 持续部署架构建议 (保留供参考)
# ========================================
# 
#  deploy-production:
#    if: github.ref == 'refs/heads/main'
#    needs: build-success
#    ... (此处保留你原文件中的 CD 配置注释，不做改动) ...