# ========================================
# SkillSpace 轻量级 CI (Lite Version)
# 目标：快速验证构建和基本逻辑，不浪费时间
# ========================================

name: SkillSpace Lite CI

on:
  push:
    branches: [ "main", "hotfix/*" ]
  pull_request:
    branches: [ "main", "feature/*" ]
  workflow_dispatch:

env:
  PYTHON_VERSION: '3.9'
  NODE_VERSION: '20'

jobs:
  # ============================
  # 任务1：前端构建检查
  # ============================
  frontend-build:
    name: 前端构建 (Vue 3)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend/skillspace
    
    steps:
      - name: 📥 检出代码
        uses: actions/checkout@v4

      - name: 🟢 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: frontend/skillspace/package-lock.json

      - name: 📦 安装并构建
        run: |
          npm ci --prefer-offline --no-audit
          npm run build

  # ============================
  # 任务2：后端代码检查
  # ============================
  backend-check:
    name: 后端检查 (SQLite)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    
    steps:
      - name: 📥 检出代码
        uses: actions/checkout@v4

      - name: 🐍 设置 Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: 📦 安装轻量依赖
        run: |
          python -m pip install --upgrade pip
          
          # 1. 简易编码修复 (使用多行模式，避免语法报错)
          python << 'EOF'
          try:
              # 尝试读取 UTF-16 (Windows)
              content = open('requirements.txt', 'r', encoding='utf-16').read()
              # 写入 UTF-8
              open('requirements.txt', 'w', encoding='utf-8').write(content)
              print('✅ UTF-16 -> UTF-8 转换成功')
          except UnicodeError:
              # 如果不是 UTF-16，说明已经是 UTF-8 或其他，不做处理
              print('ℹ️ 文件无需转换 (非 UTF-16)')
          except Exception as e:
              # 即使出错也不要中断 CI，继续尝试后续步骤
              print(f'⚠️ 编码检查跳过: {e}')
          EOF

          # 2. 过滤掉 PyTorch (节省下载时间)
          # 注意：如果你的代码 import torch 会报错，请注释掉下面这行 grep
          grep -Ev "^(torch|torchvision|torchaudio)" requirements.txt > requirements.lite.txt

          # 3. 安装业务依赖
          pip install -r requirements.lite.txt
          
          # 4. 补充安装 pymysql (防止 settings.py 报错)
          pip install pymysql

      - name: 🧪 运行快速检查
        run: |
          # 临时注入 pymysql 驱动 (防止 Django 启动报错)
          sed -i '1i import pymysql\npymysql.install_as_MySQLdb()' SkillSpace/settings.py
          
          # 设置基本密钥 (不设置 DB 变量，默认使用 SQLite)
          export SECRET_KEY='ci-lite-test-key'
          
          echo "🔍 检查代码配置..."
          python manage.py check
          
          echo "🧪 运行单元测试 (SQLite)..."
          python manage.py test